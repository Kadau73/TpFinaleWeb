@model IEnumerable<TpFinal.Models.Participant>

@{
    Layout = null;
}

<!DOCTYPE>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="stylesheet" href="/lib/bootstrap/dist/css/bootstrap.css" />
    <title>ListInscription</title>
    <style>
        body {
            background-color: #f1f1f1;
        }
    </style>
</head>
<body>
    <div class="panel-body">
        <h2>Voici la liste des gens inscrient : </h2>
        <table class="table table-sm table-striped table-bordered">
            <thead>
                <tr>
                    <th>Identifiant : </th>
                    <th>Prénom : </th>
                    <th>Nom : </th>
                    <th>Courriel : </th>
                </tr>
            </thead>
            <tbody>
                @foreach (TpFinal.Models.Participant i in Model)
                {
                <tr>
                    <th>@i.ID</th>
                    <td>@i.Prenom</td>
                    <td>@i.Nom</td>
                    <td>@i.Email</td>
                </tr>
                }
            </tbody>
        </table>
    </div>
    <div>
        <p>
            <a href="@Url.Action("Index", "Home")">Retourner</a> à la page d'acceuil?
        </p>
    </div>
</body>
</html>
